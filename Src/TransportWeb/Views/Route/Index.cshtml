@model IEnumerable<TransportWeb.Models.RouteInfo>

@{
    ViewData["Title"] = "Index";
}
<div class="row justify-content-between">
    <div class="col-4">
        <h4 class="text-success"> Route Details</h4>
    </div>
    <div class="col-2">
        <a class="btn btn-success" asp-action="AddRouteDetails"><i class="fa fa-plus-square fa-lg" aria-hidden="true"></i> Add Route</a>

    </div>
</div>

<table class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RouteNum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RouteName)
            </th>
           
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.RouteNum)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.RouteName)
                </td>
                <td>
                    @Html.ActionLink(" Edit", "EditRouteDetails", new { id=item.RouteNum },new { @class = "btn btn-warning btn-sm fa fa-pencil" })
                    @*  @Html.ActionLink("Details", "GetRouteDetails", new { id=item.RouteNum }, new{@class="btn btn-success btn-sm"}) |*@
                    @Html.ActionLink(" Delete", "DeleteRouteDetails", new { id=item.RouteNum },new{@class="btn btn-danger btn-sm fa fa-trash"})
                </td>
            </tr>
        }
    </tbody>
</table>
@if (Model.Count() == 0)
{
    <h6 class="text-success">No Data Available</h6>
}